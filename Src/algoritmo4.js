// Declara um array com valores de 1 a 5.
const arr = [1, 2, 3, 4, 5];

// Declara uma constante chamada output que guarda os valores que forem maiores que 4 no array arr.
const output = arr.filter((x) => x > 4);

// Exibe o array output no console.
console.log(output);
